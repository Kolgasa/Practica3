#include <iostream>
using namespace std;

int main() {
    system("chcp 65001");
    int a,b;
    char oper;
    cout << "введите последовательно первое, второе  числа и операцию (*/-/+/:)"<< endl;
    cin >> a >> b >> oper;
    try
    {
        if (b == 0,oper== ':')
        {
            throw b;
        }
        switch(oper)
        {
            case '*' :
                cout << a*b <<endl;
                break;
            case '+' :
                cout << a+b << endl;
                break;
            case '-' :
                cout <<"Результат операции"<< a+b <<endl;
            case ':' :
                cout <<"Результат операции"<< a/b << endl;
            defoult:
                cout << "enter a number, not a symbol "  << endl;
               
        }

    }
    catch (int b)
    {

        cout << "don’t divide by zero" << endl;

    }
    
    return 0;
}
